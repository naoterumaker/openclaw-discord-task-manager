# Discord タスク管理システム 設計思想

OpenClaw × Discord フォーラムチャンネルを使ったAIタスク管理の設計メモ。

---

## 🧠 コア思想

### 「スレッド = 指示書」

Discordフォーラムのスレッドが **タスクそのもの**。
- スレッド本文に「背景・ゴール・制約・締め切り」を書く
- ヒアリング・作業ログ・完了報告もすべてスレッド内に集約
- スレッドを読めば誰でも（どのAIでも）作業を引き継げる

### 役割分離：ディスパッチャー ↔ 実行担当

```
てるさん ──→ TERU_MASTER（ディスパッチャー）
                  ↓
              フォーラムスレッド作成
              ヒアリング開始（スレッド内で @てるさん）
                  ↓
              情報が揃ったら任意のAIが実行担当になる
              （subagent不要。スレッドを読んだAIが誰でもなれる）
```

**ディスパッチャーは振るだけ。実行はスレッドを読んだAIが引き受ける。**

---

## 🗂 チャンネル設計

| チャンネル | 役割 |
|-----------|------|
| **タスクリーダーCh** | 通知専用（開始 / エラー / 完了承認依頼のみ） |
| **フォーラムスレッド内** | 指示書・ヒアリング・作業詳細・進捗・完了報告 |

**タスクリーダーChには作業詳細を書かない。**
フォーラムスレッドだけ追えばすべてわかる設計にする。

---

## 📌 タグ設計

| タグ | 意味 |
|------|------|
| 🔴高 / 🟡中 / 🟢低 | 優先度 |
| 📚ナレッジ | 調査系（完了後もスレッドを残して再利用） |
| 完了 | てるさんの承認後にクローズ |
| ⏸ブロック中 | 依存タスク待ち |

---

## 🔄 ワークフロー

```
1. てるさん「〇〇やって」
   ↓
2. TERU_MASTERがフォーラムスレッドを作成
   → 情報が足りない場合：スレッド内で @てるさん にヒアリング
   → 情報が揃っている場合：即実行可能な指示書を書く
   ↓
3. 実行担当（スレッドを読んだAI）が作業
   → 進捗・調査結果はすべてスレッド内に投稿
   ↓
4. 完了したらタスクリーダーChに通知
   「✅ 完了承認依頼：てるさんの確認をお願いします → [スレッドURL]」
   ↓
5. てるさんが承認 → close_task.py でクローズ
```

---

## ⚙️ スキル構成

```
discord-task-manager/   ← ディスパッチャー（振り役）
  SKILL.md              ← TERU_MASTERが読む設計書
  scripts/
    add_task.py         ← スレッド作成 + 自動ヒアリング投稿
    close_task.py       ← 完了タグ付け（アーカイブなし）
    list_tasks.py       ← 未完了タスク一覧
    morning_summary.py  ← 朝サマリー（cron 8:10）
    evening_summary.py  ← 夜サマリー（cron 22:00）
    config.py           ← ID・タグID管理

task-executor/          ← 実行担当（誰でもなれる）
  SKILL.md              ← スレッドを読んで自律実行するフロー
```

---

## 🤔 設計上の決断と理由

### subagentに依存しない設計

当初はsessions_sendでフォーラムスレッドにメッセージを送る方式を検討したが、
`tools.sessions.visibility=tree` 制限でエラーになった。

→ **「スレッドを読んだAIが誰でも実行担当になれる」** 設計に転換。
subagentを立ち上げなくても、スレッドを渡されたAIがそのまま動ける。

### Pythonスクリプトは補助ツール

スクリプトはスレッド作成・タグ更新・一覧取得などの **機械的な操作** だけ担当。
「何をすべきか」「どう判断するか」はAIが考える。

### ヒアリングはスレッド内

メインセッションでヒアリングすると、てるさんが別のことをしているときに
コンテキストが分散する。

→ スレッド内で @メンションすることで、てるさんは自分のペースで返答できる。

### 調査系タスク（📚ナレッジ）は永続

調査結果は後で再利用できる。完了してもスレッドを消さず、
ナレッジタグを付けて残しておく。

---

## 📍 重要ID（運用メモ）

| 項目 | 値 |
|------|----|
| ギルドID | `1474320833269993475` |
| フォーラムCh（🤖-teru_masterタスク） | `1475147089871769643` |
| タスクリーダーCh | `1475156472072503377` |
| てるさんのDiscord ID | `971703824198824008` |
| タグ: 🔴高 | `1475171629565743281` |
| タグ: 🟡中 | `1475171629565743282` |
| タグ: 🟢低 | `1475171629565743283` |
| タグ: 📚ナレッジ | `1475171629565743284` |
| タグ: 完了 | `1475171629591167098` |
| タグ: ⏸ブロック中 | `1475171629591167099` |
| cron（朝8:10） | `b0075a01-33ff-4af7-82df-713456b4b0b5` |
| cron（夜22:00） | `3f39d393-30b4-4757-9ac7-1d263e6a2315` |
